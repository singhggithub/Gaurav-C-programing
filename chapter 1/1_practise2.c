#include<stdio.h> 

int main(){
     int length;
     float height;
     printf("the length of rectangle is \n");
     scanf("%d",&length);
      printf("the height of rectangle is \n");
      scanf("%f",&height);
      printf("The area of rectangle is %f",length*height);
     return 0;
}