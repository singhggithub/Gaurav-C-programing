#include<stdio.h> 
#include<math.h> // this include math.h is for using (pow) which is operand in simple word symbol 
//for power

int main(){
    int a=5 ;
    int b=6;
    printf(" the value of 5 to the power 4 is %f",pow(5,4));
     
     return 0;
}