#include<stdio.h>
// time stem 10:34
int main(){
char s[34];
printf("Enter the name :");
 gets(s);
puts(s);    //puts prints the value of gets without printf function
// printf("The name entered is %s",s);
     return 0;
}